# Claude Code 安全導入セットアップ手順

クライアントのプロジェクトにClaude Codeを導入する際の手順書です。

---

## 手順1｜テンプレートファイルをコピーする

このフォルダにある2ファイルをクライアントのプロジェクトルートにコピーします。

```
CLAUDE.md         ← Claude Codeへのルール指示（ソフトブロック）
.claudeignore     ← 機密ファイルの読み込み禁止設定（ハードブロック）
```

---

## 手順2｜CLAUDE.mdを編集する

`[プロジェクト名]` の部分をクライアントのプロジェクト名に変更します。
プロジェクト概要・デプロイ手順も合わせて記載しておくと、作業時のミスが減ります。

**CLAUDE.md に含まれる主なルール：**
- セキュリティ（.env 読み取り禁止・破壊的コマンド禁止・APIキーのチャット貼り付け禁止）
- プランモード（新機能・DB変更・API連携など複雑なタスクは計画を先に確認）
- Git コミット（タスク完了のたびに commit + push を提案）
- コマンド実行前の説明（実行前に何をするか・リスクは何かを日本語で説明してから確認を取る）

---

## 手順3｜.gitignoreを確認する

`.env` 系ファイルがGitHubに上がらないよう除外されているか確認します。
`.gitignore` がない場合は新規作成して以下を追加してください。

```
.env
.env.local
.env*.local
```

---

## 手順4｜APIキーの管理を確認する

| 確認項目 | 理由 |
|---|---|
| `.env.local` にキーを入れているか | コードに直書きしないため |
| GitHubにpushしていないか | 数分以内に第三者に発見される |
| 本番キーとテストキーを分けているか | 事故時の被害を最小化するため |
| APIキーに利用制限をかけているか | 不正利用時の被害を抑えるため |
| APIキーをチャットに貼り付けていないか | プロンプトインジェクションで漏洩するリスクがある |

---

## 手順5｜動作確認

プロジェクトフォルダで Claude Code を起動し、以下を確認：

- `.env.local` の内容が読み取れないことを確認（「読めません」と返ってくればOK）
- 簡単なタスクを依頼してプランモードの提案が出るか確認
- コマンド実行前に日本語で説明が出るか確認

---

## 手順6｜クライアントへの操作説明

導入完了後、クライアントに必ず以下を口頭で伝える。

### ① 許可画面の操作ルール（最重要）

Claude Codeは操作のたびに「このコマンドを実行していいか」と確認してきます。

```
[y] Yes（今回だけ許可）  [a] Always allow（自動許可）  [n] No（拒否）
```

**`a`（Always allow）は絶対に押さないこと。** 押した瞬間から確認なしでコマンドが実行され続けます。

- 分かるものは `y`
- 分からないものは `n` を押して「このコマンドは何をしますか？」と聞く
- AIが日本語で説明してくれるので、理解してから `y` を押す

### ② 特にこの単語が出たら必ず止めて確認する

| コマンドに含まれる語 | 意味 |
|---|---|
| `rm` | ファイル・フォルダの削除 |
| `DROP` / `DELETE` | データベースのデータ削除 |
| `--force` | 強制上書き |
| `curl` / `wget` | 外部への通信・ダウンロード |
| `chmod` | ファイル権限の変更 |

### ③ APIキーはチャットに貼らない

「APIキーを教えてください」と言われてもチャットには貼らない。
悪意あるサイトを経由してキーが盗まれる（プロンプトインジェクション）リスクがあります。
`.env.local` ファイルに書く方法を使うこと。

### ④ 野良スキルはインストールしない

XやSNSで「このスキルを入れると便利」と紹介されている非公式スキルは入れないこと。
ブラウザのログイン情報を外部に送信するコードが仕込まれている可能性があります。
Anthropic（Claude公式）が提供するものだけ使うこと。

---

## この設定で防げる事故

- `.env` をClaude Codeが読んで認証情報がログに漏れる
- `rm -rf` や `DROP TABLE` を誤って実行してデータが消える
- `git push --force` で他人のコミットを上書きする
- APIの無限リトライで高額請求が発生する
- 野良スキルのインストールによるデータ漏洩
- プロンプトインジェクションによるAPIキー流出
- 意味を理解せず `a`（自動許可）を押し続けることによる暴走

---

## 補足：2種類のブロックの違い

| | CLAUDE.md | .claudeignore |
|---|---|---|
| 仕組み | Claudeへのお願い | ファイルが見えなくなる |
| 強さ | 弱い（無視される可能性あり） | 強い（物理的に読めない） |
| 用途 | 行動ルール全般 | 機密ファイルの保護 |

**両方セットで使うことで二重に守れます。**
