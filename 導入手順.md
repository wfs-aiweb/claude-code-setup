# Claude Code 安全導入セットアップ手順

クライアントのプロジェクトにClaude Codeを導入する際の手順書です。

---

## 手順1｜テンプレートファイルをコピーする

このフォルダにある2点をクライアントのプロジェクトルートにコピーします。

```
.claude/settings.json  ← 機密ファイルの読み取りを拒否（ハードブロック・最優先）
CLAUDE.md              ← Claude Codeへの行動ルール指示（ソフトブロック）
```

※ `.claude/settings.json` には、コード脆弱性チェック（公式プラグイン）を有効化する設定も含まれています（手順5）。

---

## 手順2｜CLAUDE.mdを編集する

`[プロジェクト名]` の部分をクライアントのプロジェクト名に変更します。
プロジェクト概要・デプロイ手順も合わせて記載しておくと、作業時のミスが減ります。

**CLAUDE.md に含まれる主なルール：**
- セキュリティ（.env 読み取り禁止・破壊的コマンド禁止・APIキーのチャット貼り付け禁止）
- プランモード（新機能・DB変更・API連携など複雑なタスクは計画を先に確認）
- Git コミット（タスク完了のたびに commit + push を提案）
- コマンド実行前の説明（実行前に何をするか・リスクは何かを日本語で説明してから確認を取る）

---

## 手順3｜.gitignoreを確認する

`.env` 系ファイルがGitHubに上がらないよう除外されているか確認します。
`.gitignore` がない場合は新規作成して以下を追加してください。

```
.env
.env.local
.env*.local
.claude/settings.local.json
```

---

## 手順4｜APIキーの管理を確認する

| 確認項目 | 理由 |
|---|---|
| `.env.local` にキーを入れているか | コードに直書きしないため |
| GitHubにpushしていないか | 数分以内に第三者に発見される |
| 本番キーとテストキーを分けているか | 事故時の被害を最小化するため |
| APIキーに利用制限をかけているか | 不正利用時の被害を抑えるため |
| APIキーをチャットに貼り付けていないか | プロンプトインジェクションで漏洩するリスクがある |

**環境変数ファイルの命名ルール（クライアントに必ず伝える）：**

- 読み取りブロックはファイル名の列挙式。`.env.example`（値が空のサンプル）はClaude Codeが読める設計
- そのため `.env.example` / `.env.sample` に**本物の値を絶対に入れない**（変数名だけ書く）
- 本物のキーは `.env` `.env.local` `.env.production` など**標準的な名前のファイルのみ**に入れる。独自名（`.env.hoge` など）はブロック対象外になるため作らない

---

## 手順5｜コード脆弱性チェック（公式プラグイン）を有効化する

AIが書いたコードの脆弱性（インジェクション・XSS・SSRF・ハードコードされた秘密情報など）を
自動チェックする、Anthropic公式・無料のプラグイン「Security Guidance」を有効にします。
`.claude/settings.json` をコピー済みなら、プロジェクトを開いた時点で自動的に読み込まれます。

**前提：** Python 3.8以上が必要です。次のコマンドで確認します。

```bash
python --version
# 表示されない場合は python3 --version も試す。3.8以上が出ればOK
```

入っていなければ https://www.python.org からインストールしてください。

**手動で有効化する場合（確実に入れたいとき）：**

```text
/plugin install security-guidance@claude-plugins-official
/reload-plugins
```

導入範囲を聞かれたら「ユーザー（user）」を選ぶと、このPCの全プロジェクトで有効になります。

> このチェックは「書くたび（無料・モデル不使用）」「作業の区切りごと」「保存（コミット）のたび」の
> 3段階で自動実行されます。AIが見直す部分はClaudeの利用量を少し消費します。
> 池田さん自身のPCでも有効化しておくと、クライアント案件のコード品質が一段上がります。

---

## 手順6｜動作確認

プロジェクトフォルダで Claude Code を起動し、以下を確認：

- `.env.local` の内容が読み取れないことを確認（「読めません」と返ってくればOK）
- `.env.example` がある場合は、読み取れることを確認（こちらは読めるのが正常）
- 簡単なタスクを依頼してプランモードの提案が出るか確認
- コマンド実行前に日本語で説明が出るか確認
- `/plugin` でSecurity Guidanceが有効（トグルON）になっているか確認

---

## 手順7｜クライアントへの操作説明

導入完了後、クライアントに必ず以下を口頭で伝える。

### ① 許可画面の操作ルール（最重要）

Claude Codeは操作のたびに「このコマンドを実行していいか」と確認してきます。

```
[y] Yes（今回だけ許可）  [a] Always allow（自動許可）  [n] No（拒否）
```

**`a`（Always allow）は絶対に押さないこと。** 押した瞬間から確認なしでコマンドが実行され続けます。

- 分かるものは `y`
- 分からないものは `n` を押して「このコマンドは何をしますか？」と聞く
- AIが日本語で説明してくれるので、理解してから `y` を押す

### ② 特にこの単語が出たら必ず止めて確認する

| コマンドに含まれる語 | 意味 |
|---|---|
| `rm` | ファイル・フォルダの削除 |
| `DROP` / `DELETE` | データベースのデータ削除 |
| `--force` | 強制上書き |
| `curl` / `wget` | 外部への通信・ダウンロード |
| `chmod` | ファイル権限の変更 |

### ③ APIキーはチャットに貼らない

「APIキーを教えてください」と言われてもチャットには貼らない。
悪意あるサイトを経由してキーが盗まれる（プロンプトインジェクション）リスクがあります。
`.env.local` ファイルに書く方法を使うこと。

### ④ 野良スキルはインストールしない

XやSNSで「このスキルを入れると便利」と紹介されている非公式スキルは入れないこと。
ブラウザのログイン情報を外部に送信するコードが仕込まれている可能性があります。
Anthropic（Claude公式）が提供するものだけ使うこと。

---

## この設定で防げる事故

- `.env` をClaude Codeが読んで認証情報がログに漏れる
- `rm -rf` や `DROP TABLE` を誤って実行してデータが消える
- `git push --force` で他人のコミットを上書きする
- APIの無限リトライで高額請求が発生する
- 野良スキルのインストールによるデータ漏洩
- プロンプトインジェクションによるAPIキー流出
- 意味を理解せず `a`（自動許可）を押し続けることによる暴走
- AIが書いたコードに脆弱性（インジェクション・XSS等）が作り込まれる

---

## 補足：2層＋コード検査で守る仕組み

| | .claude/settings.json | CLAUDE.md |
|---|---|---|
| 仕組み | 読み取り操作を拒否する | Claudeへのお願い |
| 強さ | 強い（読み取り自体をブロック） | 弱い（無視される可能性あり） |
| 用途 | 機密ファイルの保護（最優先） | 行動ルール全般 |

機密ファイルを「読ませない」保証は `.claude/settings.json` の読み取り拒否ルールが担います。
何を守っているかは settings.json の deny 一覧をそのまま見れば確認できます。

※ かつて同梱していた `.claudeignore` は、Claude Code の公式機能ではなく保護効果がないため廃止しました。

## 補足：もう一段の守り（コード脆弱性の自動チェック）

上記2層は「情報を読ませない・壊させない」というアクセス制御の守りです。
これに加えて、公式プラグイン **Security Guidance** が「AIの書いたコードに脆弱性がないか」を
自動チェックします（手順5）。アクセス制御とコード検査の両面で守る構成です。

| | アクセス制御（settings.json 等） | コード検査（Security Guidance） |
|---|---|---|
| 守る対象 | 情報漏洩・破壊的操作 | 生成されたコードの脆弱性 |
| 例 | `.env`を読ませない／削除を止める | インジェクション・XSS・SSRF等を検出 |
| 提供 | w.f&s 設定 | Anthropic公式・無料 |
