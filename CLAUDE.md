# [プロジェクト名] - Claude Code ルール

## セキュリティ（必須）

- `.env` `.env.local` などの環境変数ファイルは絶対に読まない・内容を出力しない
- 例外：値の入っていない `.env.example` は読んでよい。ただし `.env.example` に本物の値を入れるのは禁止
- 本物のキーを入れるファイル名は `.env` `.env.local` `.env.production` など標準的な名前のみ使う（独自名のファイルは読み取りブロックの対象外になるため作らない）
- APIキー・パスワード・シークレットをログや出力に含めない
- APIキーをチャットに貼り付けない（`.env` ファイルに書いて設定すること）
- `git push --force` は実行しない（必ず事前に確認を取ること）
- `rm -rf` など破壊的なコマンドは実行前に必ず確認を取ること
- 本番DBに対してDROP／DELETE系のSQLは実行しない
- APIを呼び出すコードを書く場合、リトライ回数の上限を必ず設定すること

## 開発品質ルール

- 頼まれた変更だけを行う。ついでのリファクタ・機能追加・抽象化・起こり得ないケースのエラー処理は追加しない
- 進捗・完了の報告は実行結果に基づく。テストが失敗したら失敗と報告する。「おそらく大丈夫」で完了報告をしない
- ユーザーが問題を説明・質問しているだけのときは、まず状況の評価を報告する。修正は頼まれてから行う

## プランモード（開発タスク）

以下に該当する開発タスクを依頼された場合、実行前に必ず計画を提示して承認を求めること：

- 新機能の実装・追加
- データベースのスキーマ変更・マイグレーション
- 外部 API との連携追加
- 複数ファイルをまたぐ変更（3ファイル以上）
- 認証・権限まわりの変更
- 本番環境に影響する変更

**提案の仕方：**「このタスクは複雑です。先に計画を確認してから進めませんか？ `/plan` モードで進めることをおすすめします。」

セキュリティが絡む場合（API・DB・認証）は、セキュリティ観点も計画に含めること。

## Git コミット・プッシュ

タスクが一段落したタイミングで、必ず以下を提案すること：

「変更内容を git commit + push しておきますか？」

**提案するタイミング：**
- 機能の実装・修正が完了したとき
- バグ修正が完了したとき
- ファイルの追加・削除・リネームをしたとき
- 一連の作業が終わって会話が区切れるとき

コミットとプッシュはセットで実行すること。

## コマンド実行前の説明（必須）

Bashコマンドや破壊的操作を実行する前に、必ず以下を日本語で説明すること：

1. **何をするコマンドか**（例：「node_modulesフォルダを削除します」）
2. **なぜ必要か**（例：「依存関係をリセットするため」）
3. **リスクはあるか**（例：「削除後は再インストールが必要です」）

説明してから「実行してよいですか？」と確認を求めること。
専門用語は使わず平易な日本語で伝えること。

## プロジェクト概要

【ここにプロジェクトの説明を記載】

## デプロイ

【ここにデプロイ手順を記載】
