# [プロジェクト名] - Claude Code ルール

## セキュリティ（必須）

- `.env` `.env.local` などの環境変数ファイルは絶対に読まない・内容を出力しない
- APIキー・パスワード・シークレットをログや出力に含めない
- `git push --force` は実行しない（必ず事前に確認を取ること）
- `rm -rf` など破壊的なコマンドは実行前に必ず確認を取ること
- 本番DBに対してDROP／DELETE系のSQLは実行しない
- APIを呼び出すコードを書く場合、リトライ回数の上限を必ず設定すること

## プロジェクト概要

【ここにプロジェクトの説明を記載】

## デプロイ

【ここにデプロイ手順を記載】
